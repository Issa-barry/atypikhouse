version: '3'
services:
  app:
    build: .
    container_name: atypikhouse
    restart: always
    ports:
      - "9001:9000"
    volumes:
      - .:/var/www/html
  # caddy:
  #   image: abiosoft/caddy:latest
  #   restart: always
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./caddy/Caddyfile:/etc/Caddyfile
  #     - /var/www/html:/srv
